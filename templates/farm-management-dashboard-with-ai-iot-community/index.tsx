/**
 * @name 农场 AIoT 管理仪表盘
 *
 * Axhub runtime adapter only.
 *
 * Keep the imported Figma Make app structure under `src/app/**`; this file only
 * connects that app to the prototype runtime.
 */
import "./style.css";
import React from "react";

import App from "./src/app/App";

export default function FarmManagementDashboardWithAiIotCommunity() {
  return <App />;
}
