# Figma Make 项目转换任务清单

> **重要**: 请先阅读 `/skills/figma-make-project-converter/SKILL.md` 了解转换规范

**名称**: wms-by-vitamin-community
**项目位置**: `src/prototypes/wms-by-vitamin-community/`
**原始文件**: `temp/figma_make-wms-by-vitamin-community-1775118249320` (仅供参考，不要修改)
**生成时间**: 4/2/2026, 4:24:09 PM

## 📊 项目概况

- 总文件数: 90
- 组件数: 56
- 页面数: 21
- ~~路径别名 (@/)~~: 0 处，已由脚本转换
- ~~版本化导入~~: 77 处，已改为裸包名
- vite 版本化 alias: 38 处（仅保留为参考信息）
- 待安装依赖: 40 个
- CSS 变量: 274 个

## 🧱 固定目录结构（必须遵守）

```text
src/prototypes/wms-by-vitamin-community/
├── index.tsx          # Axhub runtime adapter only
├── style.css          # root style bridge only
└── src/
    ├── App.tsx        # Figma export shell only
    ├── main.tsx       # Vite mount only
    ├── index.css      # Figma style bridge only
    ├── components/    # shared page implementation
    └── styles/        # shared page styles
```

要求：
- 页面真实视觉和交互主体优先沉淀在 `src/components/**`
- 根目录 `index.tsx` 只做 Axhub 运行时适配，不复制页面视觉实现
- `src/App.tsx` 只做 Figma 导出薄壳，不复制页面逻辑
- `style.css` / `src/index.css` 只做样式桥接，避免重复堆样式
- 在 `index.tsx`、`src/App.tsx`、`src/main.tsx` 顶部写职责注释，提醒后续维护者不要让入口漂移

> 若最终项目不符合这套固定结构，视为转换未完成，应继续重构后再进入后续任务。

## ✅ 转换任务（共 6 个）

### 任务 1: 确定页面入口并创建 `index.tsx`

**候选入口**:
- `src/prototypes/wms-by-vitamin-community/src/App.tsx`
- `src/prototypes/wms-by-vitamin-community/src/main.tsx`
- `src/prototypes/wms-by-vitamin-community/src/pages/` 下共有 21 个页面文件

**操作**:
1. 按照 `/skills/figma-make-project-converter/SKILL.md` 的页面组件规范创建 `index.tsx`
2. 优先使用 `src/App.tsx` 作为汇总入口；若存在多页面路由，则收敛为本项目单入口组件
3. 不保留对 Figma Make 原始 `main.tsx` 挂载逻辑的依赖
4. 在 `index.tsx` 顶部写注释，明确它只是 Axhub runtime adapter

> 若页面后续还要重新导出 `名称.fig`，请确保导出壳子 `src/App.tsx` 最终仍能表达当前页面真实内容，避免它与根目录 `index.tsx` 漂移。

### 任务 2: 创建 `style.css`

**目标**: 按约定使用 `globals.css` 作为主样式来源

**操作**:
1. 创建 `style.css`，第一行固定为 `@import "tailwindcss";`
2. 以 `src/prototypes/wms-by-vitamin-community/src/styles/globals.css` 作为主要样式来源
3. `src/prototypes/wms-by-vitamin-community/src/index.css` 仅作为视觉回归参考，不直接搬运为最终 `style.css`
4. 在 `style.css` 与 `src/index.css` 中保留注释，明确它们只是样式桥接层

### 任务 3: 清理 Figma Make 运行时耦合

**脚本已完成**:
- ~~转换 `@/` 路径别名~~ ✓ 已完成（0 处）
- ~~转换 `package@version` 导入~~ ✓ 已完成（77 处）

**仍需处理**:
- 不再依赖 `vite.config.ts` 中的 alias 作为运行前提
- 可保留 `vite.config.ts` 作为参考，但最终组件需独立运行
- 不保留原始 Vite 挂载入口与多页面路由壳层
- `src/App.tsx` 与 `src/main.tsx` 顶部要写职责注释，避免后续维护时误塞页面逻辑

### 任务 4: 收敛多页面/路由结构

**目标**: 将 Figma Make 的多页面应用收敛为本项目单入口页面组件

**操作**:
- 保留页面视觉层级与主要交互结构
- 将路由切换逻辑合并为单页面展示或局部状态切换
- 不强行保留浏览器路由壳层

### 任务 5: 安装依赖

**执行命令**:
```bash
pnpm add @radix-ui/react-accordion @radix-ui/react-alert-dialog @radix-ui/react-aspect-ratio @radix-ui/react-avatar @radix-ui/react-checkbox @radix-ui/react-collapsible @radix-ui/react-context-menu @radix-ui/react-dialog @radix-ui/react-dropdown-menu @radix-ui/react-hover-card @radix-ui/react-label @radix-ui/react-menubar @radix-ui/react-navigation-menu @radix-ui/react-popover @radix-ui/react-progress @radix-ui/react-radio-group @radix-ui/react-scroll-area @radix-ui/react-select @radix-ui/react-separator @radix-ui/react-slider @radix-ui/react-slot @radix-ui/react-switch @radix-ui/react-tabs @radix-ui/react-toggle @radix-ui/react-toggle-group @radix-ui/react-tooltip class-variance-authority clsx cmdk embla-carousel-react input-otp lucide-react react-day-picker react-hook-form react-resizable-panels recharts sonner tailwind-merge tw-animate-css vaul
```

**已排除**: `react`、`react-dom`、`next-themes`

### 任务 6: 验收测试

**执行命令**:
```bash
node scripts/check-app-ready.mjs /prototypes/wms-by-vitamin-community
```

**验收要求**: 页面正常渲染、无控制台错误、主视觉与原项目一致

## 📚 可直接利用的设计资料

- DESIGN_SYSTEM_GUIDE.md: `src/DESIGN_SYSTEM_GUIDE.md`
- TOKEN_REFERENCE.md: `src/TOKEN_REFERENCE.md`

## 📎 产物索引

- 任务清单: `.figma-make-tasks.md`
- 分析报告: `.figma-make-analysis.json`
- 转换规范: `/skills/figma-make-project-converter/SKILL.md`
