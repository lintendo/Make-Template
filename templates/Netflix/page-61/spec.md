# Netflix Core Component: Profile Creation OS Overlay (Page 61)

## Functional Reverse-Symmetry Protocol
Taking key learnings from `page-58` (`Edit Kids Profile`), `061.png` runs an identical overarching modal layout architecture but reverses the specific vectors to facilitate "Add Framework":
1. **Background Ghost Resolution**: While `page-58` originated from the `Manage Profiles` route (and thus included thick pencil overlays in the background dim), `061.png` visibly relies on the pure `Who's Watching?` layout matrix underneath. We rebuilt the background stack to natively hold `Jiho`, `Kids`, and the `Add Profile` component without pencil disruption, maintaining absolute fidelity to Netflix route physics.
2. **Foreground Construction Node**: We inject `Mobbin` explicitly as a brand new `110x110` pure vector block. Added standard text labels, but introduced two new specialized controls.

## Bespoke iOS Switch Emulation
Netflix deliberately ignores iOS defaults to maximize visual contrast over `#000`:
- Instead of the green standard, they generate an oversized transparent bounding track relying strictly on `border-[1.5px] border-white` definition constraints.
- The indicator toggle relies on a `transitional transform: translateX` layout, visually defaulting to the false/OFF side (left).
- Added `active:scale-95` to the whole label bounding box mimicking fast response Apple interface touch surfaces.

## iOS Keyboard Recalibration
Imported our bulletproof iOS Flex matrix built in `p58`. 
Updated the **Shift Action Key** vector: In standard mode, the Apple uppercase shift is natively outlined dynamically. Created an explicit `path` logic drawing `d="M12 3 l-8 8 h5 v9 h6 v-9 h5 Z"` achieving the legendary native OS icon visually mapped on `stroke-linejoin-miter` properties.
