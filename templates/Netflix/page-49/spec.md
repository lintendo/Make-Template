# Netflix Core Component: The Glitch Glyph Render (Page 49)

## Orientation Matrix Restored
In a departure from the anomalous inversion witnessed in `048.png`, screenshot `049.png` restores our original standard **Clockwise Rotation Matrix (rotate-90)**.
- The iOS Home Indicator locks onto the physical `Portrait-Left` frame edge.
- The 3D Vintage TV model natively asserts its `Engine-Bottom` (speaker panels) along this exact left alignment, harmonizing perfectly with the coordinate shift.

## Architectural Component Integration: The Fracture Event
For the screen internals, all platformer gameplay rendering engines were swapped out for a high-intensity title card.

### The Typographic Base
- Hardcoded pure text layout reading `BLACK MIRROR`. Due to our coordinate maps, plotting standard Left-to-Right CSS string arrays correctly projects into the Bottom-to-Top column layout explicitly seen in the provided screenshot.
- Soft grey color mapping prevents standard clipping and allows the visual 'screen damage' layer to organically bloom over the vectors.

### The Symbology Glyph (White Bear)
A complete procedural SVG matrix (`<GlitchGlyph />`) is suspended inside `z-[20]`. It maps the narrative "White Bear" branching flowchart symbol horizontally across our Landscape plane. Using heavily blurred `<path>` replicas layered under absolute sharp-lined `<path>` replicas, it successfully mimics physical CRT glass fractures / chaotic electron beam discharge without relying on any external image assets.
