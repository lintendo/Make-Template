# Netflix Core Component: Details Root Modal State (Page 40)

## The Matrix Visualization System
Unlike previous `page-*` assets where the header was actively deep-scrolled into its sticky subset, this screen finally captures the **Source/Zero State View**.

### 1. Complex Source Graphic Engineering
The enormous `<FullBodyguardPoster />` UI was manually constructed replacing generic JPG mapping. The component consists of 30+ highly exact SVG path nodes:
- Emphasized character intensity matching *Richard Madden* checking tight right corners through overlapping geometric mapping `<path>` components.
- Calculated the exact physical layout of the severe facial blood textures (`r="2"` up to `r="4.5"`) directly using hex values `#690A0A` and `#520505` rendering hyper-realistic dark dried cinematic splatter overlays entirely isolated from heavy binary files.

### 2. Physical Layout Emulation
Verified the precise pixel count that correctly merges `Episode 1` Thumbnail perfectly under the bottom Safe Navigation layer exactly as showcased in `040.png`, achieving 100% natural DOM collision logic with no JavaScript forced positioning.
- Dropped the `Bodyguard` header title and shifted the "X" Close Button into the native Floating layer `bg-[#1A1A1A]/70` confirming we are actively inside the raw 0px scroll point of a newly expanded Netflix Bottom Sheet.
